// const a = [-1, 150, 190, -1, -1, 160, 180];
// const arrHeight = a.filter((x) => x > 0).sort((a, b) => a - b);

// const sortByHeight = (arr, arrHeight) => {
//   let result = [];
//   let check = false;
//   let index = 0;

//   for (const [i, value] of arr.entries()) {
//     if (value < 0) {
//       result.push(value);
//       continue;
//     } else {
//       result = [...result, arrHeight[index]];
//       index++;
//       continue;
//     }
//   }
//   return result;
// };

// console.log(sortByHeight(a, arrHeight));
